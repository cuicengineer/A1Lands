/** Shared grid table header font sizes (2px smaller than prior defaults). */

/** Default header — was 0.875rem (14px). */
export const GRID_TABLE_HEADER_FONT_SIZE = "0.75rem";

/** Pages that used 1rem (16px) headers. */
export const GRID_TABLE_HEADER_FONT_SIZE_MD = "0.875rem";

/** Pages that used 1.05rem (~16.8px) headers. */
export const GRID_TABLE_HEADER_FONT_SIZE_LG = "0.925rem";

/** Pages that used 1.15rem (~18.4px) headers. */
export const GRID_TABLE_HEADER_FONT_SIZE_XL = "1rem";

/** Pages that used 12px headers. */
export const GRID_TABLE_HEADER_FONT_SIZE_PX = "10px";

export const gridTableHeaderThFontSx = {
  fontSize: `${GRID_TABLE_HEADER_FONT_SIZE} !important`,
};
